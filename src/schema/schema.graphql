type Person {
  id: ID!
  givenName: String!
  familyName: String!
  label: String!
  gender: String
  dateOfBirth: String
  dateOfDeath: String
  birthLocation: String
  deathLocation: String
  notes: String
  createdBy: String
  createdAt: String
}

type User {
  id: ID!
  email: String!
  createdAt: String!
}

type Query {
  persons: [Person]
}
